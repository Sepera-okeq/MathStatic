# Лешкевич С.А
# Вариант 6 -> MoMe 4
x <- read.csv(file="~/MathStatic/materials/6/r3z1.csv")$X

x.min <- min(x)         # Минимальное X из выборки
x.max <- max(x)         # Максимальное X из выборки

n <- length(x)          # Размер выборки (Обьем)
avg <- sum(x)/n         # Среднее значение
var <- sum(x^2)/n       # Дисперсия

#th <- 3 * avg/n;
th <- 3 * avg;

cat(
 "Задание 5:", "\n",
 "Обьём выборки:", n, "\n",
 "Среднее:", avg, "\n",
 "Дисперсия:", var, "\n",
 "Значение оценки θ:", th
)